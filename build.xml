<project name="BetterBotProfiles" basedir="." default="createAll">
	
    <property name="src.dir" value="src"/>
		<property name="dist.dir" value="dist"/>
    <property name="build.dir" value="build"/>

	<target name="clean">
		<delete dir="${build.dir}"/>
	</target>
	
	<target name="compileAll">
		<mkdir dir="${build.dir}"/>
		<javac includeantruntime="false" srcdir="${src.dir}/rotations" destdir="${build.dir}" classpath="lib/libBetterBot.jar"/>
	</target>
	
	<target name="createAllJars" depends="compileAll">
		<mkdir dir="${dist.dir}"/>
		<jar destfile="${dist.dir}/Rotations.jar" basedir="${build.dir}"/>
	</target>
	
	<target name="exportDruid">
		<mkdir dir="${build.dir}"/>
		<javac includeantruntime="false" srcdir="${src.dir}/rotations" includes="DruidSolver.java" destdir="${build.dir}" classpath="lib/libBetterBot.jar"/>
		<mkdir dir="${dist.dir}"/>
		<jar basedir="${build.dir}/rotations" destfile="${dist.dir}/DruidSolver.jar" includes="DruidSolver.class"/>
	</target>	
	
	<target name="exportHunter" depends="clean">
		<mkdir dir="${build.dir}"/>
		<javac includeantruntime="false" srcdir="${src.dir}/rotations" includes="HunterSolver.java" destdir="${build.dir}" classpath="lib/libBetterBot.jar"/>
		<mkdir dir="${dist.dir}"/>
		<jar basedir="${build.dir}/rotations" destfile="${dist.dir}/HunterSolver.jar" includes="HunterSolver.class"/>
	</target>	
	
	<target name="exportMage">
		<mkdir dir="${build.dir}"/>
		<javac includeantruntime="false" srcdir="${src.dir}/rotations" includes="MageSolver.java" destdir="${build.dir}" classpath="lib/libBetterBot.jar"/>
		<mkdir dir="${dist.dir}"/>
		<jar basedir="${build.dir}/rotations" destfile="${dist.dir}/MageSolver.jar" includes="MageSolver.class"/>
	</target>
	
	<target name="exportPaladin">
		<mkdir dir="${build.dir}"/>
		<javac includeantruntime="false" srcdir="${src.dir}/rotations" includes="PaladinSolver.java" destdir="${build.dir}" classpath="lib/libBetterBot.jar"/>
		<mkdir dir="${dist.dir}"/>
		<jar basedir="${build.dir}/rotations" destfile="${dist.dir}/PaladinSolver.jar" includes="PaladinSolver.class"/>
	</target>
	
	<target name="exportPriest">
		<mkdir dir="${build.dir}"/>
		<javac includeantruntime="false" srcdir="${src.dir}/rotations" includes="PriestSolver.java" destdir="${build.dir}" classpath="lib/libBetterBot.jar"/>
		<mkdir dir="${dist.dir}"/>
		<jar basedir="${build.dir}/rotations" destfile="${dist.dir}/PriestSolver.jar" includes="PriestSolver.class"/>
	</target>
	
	<target name="exportRogue">
		<mkdir dir="${build.dir}"/>
		<javac includeantruntime="false" srcdir="${src.dir}/rotations" includes="RogueSolver.java" destdir="${build.dir}" classpath="lib/libBetterBot.jar"/>
		<mkdir dir="${dist.dir}"/>
		<jar basedir="${build.dir}/rotations" destfile="${dist.dir}/RogueSolver.jar" includes="RogueSolver.class"/>
	</target>
	
	<target name="exportShaman">
		<mkdir dir="${build.dir}"/>
		<javac includeantruntime="false" srcdir="${src.dir}/rotations" includes="ShamanSolver.java" destdir="${build.dir}" classpath="lib/libBetterBot.jar"/>
		<mkdir dir="${dist.dir}"/>
		<jar basedir="${build.dir}" destfile="${dist.dir}/ShamanSolver.jar" includes="rotations/ShamanSolver.class"/>
	</target>
	
	<target name="exportWarrior">
		<mkdir dir="${build.dir}"/>
		<javac includeantruntime="false" srcdir="${src.dir}/rotations" includes="WarriorSolver.java" destdir="${build.dir}" classpath="lib/libBetterBot.jar"/>
		<mkdir dir="${dist.dir}"/>
		<jar basedir="${build.dir}/rotations" destfile="${dist.dir}/WarriorSolver.jar" includes="WarriorSolver.class"/>
	</target>
	
	<target name="createAll" depends="createAllJars,exportDruid,exportHunter,exportMage,exportPaladin,exportPriest,exportRogue,exportShaman,exportWarrior"/>
	
</project>